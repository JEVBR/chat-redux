// reducers/channels_reducer.js

const channelsReducer = (state, action) => {
  if (state === undefined) {
    return [];
  }

  switch (action.type) {
    default:
      return state;
  }
};

export default channelsReducer;

