// reducers/currentUser_reducer.js

const currentUserReducer = (state, action) => {
  if (state === undefined) {
    return [];
  }

  switch (action.type) {
    default:
      return state;
  }
};

export default currentUserReducer;

